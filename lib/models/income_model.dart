import 'package:flutter/material.dart';

class IncomeModel {
  final Color dotColor;
  final String title, percentage;

  const IncomeModel(
      {required this.dotColor, required this.title, required this.percentage});
}
